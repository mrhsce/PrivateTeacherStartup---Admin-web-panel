import {Enum} from "./Enum";

export class Student {
    Id: number;
    Name: string;
    Family: string;
    Gender: boolean;
    PhoneNumber: string;
    UserId: string;
    UserName: string;
    Status: Enum;
}
